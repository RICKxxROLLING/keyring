// server/realtime/register.ts — CONTRACT STUB. Owner: T2. Replaced at integration.
import type { FastifyInstance } from "fastify";
import type { AppContext } from "../context.js";

export async function registerRealtime(_app: FastifyInstance, _ctx: AppContext): Promise<void> {}
