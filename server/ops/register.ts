// server/ops/register.ts — CONTRACT STUB. Owner: T5. Replaced at integration.
import type { FastifyInstance } from "fastify";
import type { AppContext } from "../context.js";

export async function registerOps(_app: FastifyInstance, _ctx: AppContext): Promise<void> {}
