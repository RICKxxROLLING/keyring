// server/domain/register.ts — CONTRACT STUB. Owner: T3. Replaced at integration.
import type { FastifyInstance } from "fastify";
import type { AppContext } from "../context.js";

export async function registerDomain(_app: FastifyInstance, _ctx: AppContext): Promise<void> {}
