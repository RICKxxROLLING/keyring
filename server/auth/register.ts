// server/auth/register.ts — CONTRACT STUB. Owner: T1. Replaced at integration.
import type { FastifyInstance } from "fastify";
import type { AppContext } from "../context.js";

export async function registerAuth(_app: FastifyInstance, _ctx: AppContext): Promise<void> {}
