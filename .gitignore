node_modules/
dist/
.env
*.db
*.db-shm
*.db-wal
# Runtime state lives under DATA_DIR. Anchored with a leading slash: the bare
# patterns "uploads/" and "backups/" also matched server/uploads/ (source code),
# silently excluding it from `git add`. See .crew/*/known-defects.md D6.
/data/
.crew/

# T5 additions — append only, never remove a line above.
.env.local
*.log
coverage/
*.tar.gz.enc
setup-token.txt
.DS_Store
Thumbs.db
.claude/
