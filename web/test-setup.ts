// Placeholder — owner T4. Created empty here only so `vitest run` does not
// fail to resolve `setupFiles` in a worktree that does not yet contain T4's code.
// T4's real version (importing @testing-library/jest-dom/vitest) replaces this wholesale.
export {};
